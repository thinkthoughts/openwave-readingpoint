# M8 / MIT, ROADMAP

> The M8 model's task roadmap. The program is a **field-dynamics collaboration**: MIT supplies the arena (S³/2I + the Möbius edge) and the target spectrum (the McKay ladder); the platform supplies Lagrangian candidates, simulation engineering, and grading standards. Live work: [IN PROGRESS](#in-progress) → [BACKLOG](#backlog) → [LATER](#later-gated); the validated record: [DONE](#done). Reading rules (spec of record, mode of work, standing rules): [§ CONVENTIONS](#conventions). Dated scaffold, ruling, and close-out records: [§ CHANGE-LOG](#change-log).

---

## IN PROGRESS

> The single currently-running task (one at a time; empty = between tasks). The author drives the column; maintainer-run tasks are labeled in the Owner column of the Backlog.

| TaskID | Title | Description | Gated By |
| --- | --- | --- | --- |

## BACKLOG

> Future tasks, each linking its [`tasks/m8_<id>_task_details.md`](tasks/) with the full planning detail. Ownership is per row. **Top of the list is next up**, subject to `Gated By`; the author drives the column and is free to pick a different row.

| TaskID | Title | Description | Owner | Gated By |
| --- | --- | --- | --- | --- |
| [M8.5](tasks/m8_5_task_details.md) | Quotient-manifold simulation engineering | Simulate on S³/2I via (a) a 2I-equivariant grid or (b) a spectral method in 2I-symmetric harmonics; prototype both, choose on evidence (pointer map § 6). Carries the M8.2 lock § 3 reproduction obligation. **M8.5-A** ✅ REPRODUCED (2026-07-31, clean-room; [method note](findings/m8_5a_method_note.md) + § J audit), and **M8.5-B** the quotient backend is ✅ COMPLETE: sealed case `M85B-ADJ-07` adjudicated GREEN at both rungs, 2026-08-17 ([#446](https://github.com/openwave-labs/openwave/pull/446)) | protocol: author; reproduction: maintainers; simulation prototypes: author + platform support | M8.2 ✅ (2026-07-27); [protocol](findings/m8_5a_reproduction_protocol.md) ✅ LOCKED 2026-07-30; M8.5-A ✅ (2026-07-31); [B pre-registration](findings/m8_5b_preregistration.md) ✅ LOCKED 2026-08-01; M8.5-B adjudication ✅ (2026-08-17, `M85B-ADJ-07`) |
| [M8.4](tasks/m8_4_task_details.md) | Lagrangian-family survey on S³/2I | The central question, reshaped by the task's first result: single-valued fields on S³/2I carry no nontrivial McKay slot at any level ([kinematic close](findings/m8_4_kinematic_close.md)), closing OQ1's native branch by theorem. The slot survey moves to the M8-owned twisted object `M4_int` across the three frozen flat connections, `σ_0` the mandatory null control; native stages survive as an optional descriptive survey | author, platform support | M8.2 ✅ (2026-07-27), M8.5 ✅ (A 2026-07-31, B 2026-08-17); kinematic close ✅ (2026-08-18); the `M4_int` preregistration is next |

## LATER (gated)

> Held behind a hard gate, not merely unscheduled: the gate is a result, not a date.

| TaskID | Title | Description | Gate |
| --- | --- | --- | --- |
| [M8.7](tasks/m8_7_task_details.md) | The 3D rendering port | Port the M5-style interactive stack (per-model `_launcher.py` + `engine1-4` split + the shared GGUI rendering in `openwave/i_o/`) so the validated S³/2I dynamics runs as a live demo. Do NOT start this before the gate: rendering an unvalidated dynamics showcases nothing. Porting instructions for AI agents: [`m8_platform_pointers.md § 7`](m8_platform_pointers.md) | field dynamics validated in-platform (an M8.4-lineage result with an audited method note) |

---

## STATUS AT A GLANCE (2026-08-22)

| Question | Answer |
| --- | --- |
| Where is M8? | Scaffold merged AND the certification gate passed: M8.1 ✅ (2026-07-21) verified the arena's headline eigenvalue theorem at 10-digit precision (blind two-agent, audited), flipping the first MODELS.md cell (gravity → ⚠️). M8.2 ✅ (2026-07-27) then locked the field-dynamics pre-registration, the author's first contribution through the normal fork → branch → PR flow ([`CONTRIBUTING.md`](../../../../CONTRIBUTING.md), DCO). M8.1.1 ✅ (2026-07-28) then verified the two REMAINING bedrock papers blind, so all three of the arena's math papers are now independently checked in-platform, with the coexact-gap classification (4/R², one 36/R² exception) as its contingent result and one platform-side overclaim corrected. M8.3 ✅ (2026-07-28) landed the mass-formula reproducer, whose own reproduction caught a defect in the published page and corrected it upstream. M8.5-A ✅ REPRODUCED (2026-07-31): the author's packet went through the clean-room pipeline (protocol LOCKED 2026-07-30, [change-log entry](#change-log)), 27/27 cells against the pinned § 6.1 with three-way agreement, the § 6 coexact verdict `structurally derived and reproduced` surviving the recorded adversarial audit, so M8.5-B is the live remainder and M8.4's gate is closer. M8.6 is GATED (2026-07-29; readiness audit: target circular, fallback non-independent) and since 2026-08-07 CLOSED on the M5 side (the route-(b) ladder failed terminally, [finding 9](tasks/m8_6_task_details.md)). M8.8's protocol and construction packet MERGED 2026-08-11 ([#408](https://github.com/openwave-labs/openwave/pull/408)), review-widened so the universal-cover gate demands an exact saturation certificate in every degree; [#430](https://github.com/openwave-labs/openwave/pull/430) then filled § 11's pins and corrected the § 10.3 packaging clause pre-lock; the freeze bound at the lock commit, five commissions ran, and M8.8 ✅ ADJUDICATED 2026-08-22 under the `convention difference` success category (closed out with the author's provenance layer, [#459](https://github.com/openwave-labs/openwave/pull/459)), the closed forms reproduced exactly by a fresh context from the based chain complex ([record](findings/m8_8_adjudication_record.md)) |
| What kind of column is it? | The platform's first top-down structural model: strong on the origin of the numbers (representation theory on S³/2I), absent on dynamics. The M8 program exists to supply the dynamics half |
| What decides the program? | M8.1 ✅ certified the arena's headline eigenvalue (the certification-gate philosophy paid off: both blind agents re-derived the paper's constants 2/e and −4e^(−2γ) to 10 digits without seeing them); M8.2 ✅ then froze what M8.4 will be graded against, before any numerics existed to tune it toward; M8.4 is the decisive science (does ANY reasonable Lagrangian on S³/2I realize the McKay slot structure?); M8.6 closed on the M5 side 2026-08-07 (its bridge's last admissible route failed terminally), so the program's payoff rests on M8.4/M8.5 |
| Evidence discipline | The author's own claim ledger is adopted as the grading baseline (structural results = the core; the numeric mass table = low weight, capped by the author's own pre-registered nulls); platform standards (pre-registration, adversarial audit, method notes) apply from day one |

## CONVENTIONS

> Standing reading rules for this doc. Dated events live in [§ CHANGE-LOG](#change-log).

**Spec of record + reading map.** [`m8_theory_canonical.md`](m8_theory_canonical.md) is the FIRST READ and is canonical when docs disagree. Rationale and gap map: [`m8_background.md`](m8_background.md); cross-model reading map: [`m8_platform_pointers.md`](m8_platform_pointers.md). AI agents bootstrap via [`m8_agent_orientation.md`](m8_agent_orientation.md) ("read the m8_agent_orientation.md"), then run tasks with "go task m8.\<n\>".

**Mode of work.** Research mode FIRST: headless scripts + research notes + plots, no GUI (the suggested per-task layout: `tasks/m8_<id>_task_details.md`, scripts / data / plots under `research/` with `m8_<id>_` prefixes). The 3D rendering port (M5-style interactive launcher) is a LATER stage, gated on field dynamics validating in-platform: [M8.7](#later-gated), pointers in [`m8_platform_pointers.md § 7`](m8_platform_pointers.md).

**Standing rules (platform-standard, from day one).** Pre-register gates and conventions BEFORE each run; no calibrated conventions (derive and pre-register, or record as a fit with its search space); honest negatives are results; substantive claims get an adversarial audit ([`AI_HYGIENE.md`](../../../../AI_HYGIENE.md) § 1); author-facing reports follow [`dev_docs/METHOD_NOTE.md`](../../../../dev_docs/METHOD_NOTE.md) (equations first + equation-to-code map). Ownership is marked per task: the author drives the column; maintainer-run tasks are labeled.

**Self-checks must be able to fail (M8.2, 2026-07-27).** Any line a script prints as PASS should be mutation-tested before it ships: change the thing it checks to something wrong and confirm the check goes red. A check whose two sides evaluate the same expression always passes and reads, to a later maintainer, exactly like a verified result. M8.2 shipped one (the coexact table's trivial column compared against the very rule that produced it) and it was replaced at merge by two that fail under mutation: `dims` against the affine E8 mark condition, and `dist` against BFS on the McKay graph. Where a table has no independent published target to check against, label it ASSERTED rather than giving it a self-check that cannot discriminate.

**Blind vs independent-method verification (M8.2 close-out, 2026-07-28).** Blind means the verifying agent has not seen the claimed values ([`ONBOARDING_MODELS.md § 3.2`](../../../../ONBOARDING_MODELS.md#32-the-maintainer-sequence) step 5). A context that has already seen the targets can still verify by an independent method, but the result is reported as **independent-method reproduction**, never as blind; the ceiling on the claim is set by what the verifying context has seen, not by how separate the implementation is. M8.1 was blind (targets withheld from both agents) and M8.1.1 is blind by construction. M8.5's table reproduction cannot be: the context that produced M8.2 holds the fixtures, and the maintainer reproduced the tables during the [PR #350](https://github.com/openwave-labs/openwave/pull/350) review. The M8.2 lock's § 3 requirement (independently implemented, may compare against the script but not call it, import its tables, or share its derived fixtures) is IMPLEMENTATION independence and stands unchanged; the author's added firewall extends it to the CONTEXT (implement with no M8.2 internals loaded), which is the part that AI-run work can silently violate.

**Three-way agreement (M8.2 close-out, 2026-07-28).** Where a claim ends up with three artifacts computing it by different methods (a generator, a review-time reconstruction, and a fresh-context reproduction), agreement among all three is reported as **three-way agreement**. It is a provenance statement, not a stronger verification label: it does not move a result off independent-method reproduction, since the ceiling is set by what the verifying contexts have seen. Corollary for review work: a verification that stays a throwaway cannot be quarantined, cited, or re-run by anyone else, so land it as an artifact with source, environment, raw output, the commit verified against, a method note, mutation tests for every PASS line, and a statement of what it does NOT verify. First instance: [`scripts/m8_2_indep_reconstruction.py`](scripts/m8_2_indep_reconstruction.py) + [`findings/m8_2_indep_reconstruction_note.md`](findings/m8_2_indep_reconstruction_note.md).

**Borrowing other columns' families (platform ruling, 2026-07-24).** The M8 program tests candidate families drawn from M4 / M5 / M7 inside the MIT arena, so it runs under [`dev_docs/CROSS_MODEL_TESTING.md`](../../../../dev_docs/CROSS_MODEL_TESTING.md): a borrowed family is native and untwisted unless its author declares an internal representation or a soldering prescription is pre-registered; "not applicable" is a neutral status; a soldered family is scored as its own object; author silence is a valid terminal state and never blocks a pre-registration lock. Author-gated questions go from author to author directly (Q&A discussion), not through a maintainer relay.

---

## DONE

> Newly done tasks APPEND at the end. The full per-task record lives in `tasks/m8_<id>_task_details.md`; the row is the summary.

| TaskID | Title | Description | Completed |
| --- | --- | --- | --- |
| (scaffold) | The M8 column scaffold | Onboarding evaluation (provenance + § 1 / § 4 / § 5.1 checks) passed; [`__M8_model_briefing.md`](../__M8_model_briefing.md) + [`m8_background.md`](m8_background.md) + [`m8_platform_pointers.md`](m8_platform_pointers.md) + [`../theory/_CITATIONS.md`](../theory/_CITATIONS.md) written; MODELS.md column added at 21 🚧 | 2026-07-21 |
| [M8.1](tasks/m8_1_task_details.md) | THE CERTIFICATION GATE: independent eigensolve of the twisted Möbius Laplacian | ✅ PASSED, all five pre-registered claims CONFIRMED across two mutually independent blind implementations (≤ 1.4e-8): λ₁⁺ = 2/R² and α₀(α₀+1)/R² exact, the extension-stability threshold bisected blind to 2R/e, the bridging coefficient to −4e^(−2γ), Friedrichs zero mode exact with exactly one bridging bound state. Audit 6/6. Canonical § 3, briefing and the MODELS.md gravity cell synced. [Method note](findings/m8_1_method_note.md) | 2026-07-21 |
| [M8.2](tasks/m8_2_task_details.md) | Pre-registration lock for the field-dynamics program | ✅ LOCKED: [`findings/m8_2_preregistration.md`](findings/m8_2_preregistration.md), a modular contract (immutable core, per-family modules, signed execution appendices). Targets = the structural ladder only; four-axis outcomes; no-search rule; provenance pinned and audited. The index question became the platform rule [`CROSS_MODEL_TESTING.md`](../../../../dev_docs/CROSS_MODEL_TESTING.md). Table independently reproduced 9/9 ([script](scripts/m8_2_indep_reconstruction.py) + [note](findings/m8_2_indep_reconstruction_note.md)), 8 mutation-tested checks, 0-form only so the coexact rule stays ASSERTED. Author-contributed via [PR #350](https://github.com/openwave-labs/openwave/pull/350) | 2026-07-27 |
| [M8.3](tasks/m8_3_task_details.md) | Mass-formula reproducer script | ✅ COMPLETE, author-contributed via [PR #362](https://github.com/openwave-labs/openwave/pull/362). Every constant recomputed from its definition, 23 gates with a coverage-guarded mutation registry (all red). The reproduction FOUND A DEFECT in the published page (a dropped scalar-zeta term for half-integer bundles); corrected upstream, the null re-run as v1.1 at p_A = 0.690, and no MODELS.md icon moved. [Method note](findings/m8_3_method_note.md) | 2026-07-28 |
| [M8.1.1](tasks/m8_1_1_task_details.md) | Second blind run: the remaining bedrock theorems | ✅ BOTH PAPERS VERIFY. 18 pre-registered claims (A1-A7, B1-B11) confirmed blind, two adversarial audits refuting nothing (34 and 37 groups, 6912 twists). The adjoint coexact gap is 4/R² across the ADE family with exactly one exception, 36/R², uniqueness held over 41 connections; the affine conversion paper's arithmetic reproduces exactly. ⚠️ That conversion identity is algebraically forced, a platform-side overclaim corrected at review. [Method note](findings/m8_1_1_method_note.md) | 2026-07-28 |
| [M8.6](tasks/m8_6_task_details.md) | MIT-M5 lepton-hierarchy comparison (gated readiness audit) | ❌ CLOSED WITHOUT RUNNING, row retired to Done 2026-08-08 (maintainer call). Deliverable = the readiness audit + gate record: the named target proved circular (2026-07-29), the amended condition's last admissible route (M5.21.11 route (b)) failed terminally (2026-08-07), so the comparison is permanently inadmissible as instrumented ([finding 9](tasks/m8_6_task_details.md)) | 2026-08-07 |
| [M8.8](tasks/m8_8_task_details.md) | Independent-method reproduction of the M8.3 torsion closed forms | ✅ REPRODUCED under the protocol's `convention difference` category (2026-08-22), closed out with the author's provenance layer ([#459](https://github.com/openwave-labs/openwave/pull/459)): a context-isolated fresh implementer rebuilt the torsion from the based chain complex; the § 8 adjudication found 8/8 rows and 4/4 identities exact in `Q(φ)` under the global inverse at `R7`. Attempt 1, a `structural failure` on a one-string interface mismatch, stays on record; [record](findings/m8_8_adjudication_record.md) | 2026-08-22 |

## CHANGE-LOG

> Dated scaffold, ruling, and close-out records, chronological (oldest first). Per-task detail lives in the `tasks/` files; these entries are the roadmap-level routing.

**SCAFFOLDED (2026-07-21).** The M8 column was scaffolded by the maintainers from the author's onboarding proposal ([discussion #312](https://github.com/openwave-labs/openwave/discussions/312)), following the maintainer sequence in [`ONBOARDING_MODELS.md § 3.2`](../../../../ONBOARDING_MODELS.md#32-the-maintainer-sequence). The file set, the MODELS.md column at 21 🚧, and the M8.1 blind certification gate all landed the same day (see [DONE](#done)).

**PLATFORM RULING (2026-07-24).** The cross-model index question raised inside M8.2 was answered platform-wide rather than in this column: it became [`dev_docs/CROSS_MODEL_TESTING.md`](../../../../dev_docs/CROSS_MODEL_TESTING.md), and two model authors then declared in confirmation of its default ([#333](https://github.com/openwave-labs/openwave/discussions/333) M4, [#334](https://github.com/openwave-labs/openwave/discussions/334) M5). Standing form: [§ CONVENTIONS](#conventions), "Borrowing other columns' families".

**M8.1.1 CLOSE-OUT: THE BEDROCK SET IS COMPLETE (2026-07-28).** The second blind run verified the remaining two bedrock papers, so all three math papers under the arena are now checked in-platform by agents that never saw a claimed value. Two blind solvers, two adversarial audits by deliberately different methods (exact finite-field arithmetic; groups from presentations plus a field-free rational route), 18 pre-registered claims, nothing refuted. The audits earned their cost twice: they found four checks in the solvers that could not fail, one mutation that ran green with the wrong Dynkin diagram installed, and, most importantly, that the affine conversion identity is algebraically FORCED rather than an empirical law. The last point corrected a platform-side overclaim in the run's own method note, not anything the author wrote: the paper calls that theorem elementary. Canonical status rows for both papers flip; the MODELS.md confinement icon deliberately does NOT move, because a verified spectral gap at a flat connection is not the linear inter-charge potential that row tests.

**AUTHOR'S FIRST PR + M8.2 CLOSE-OUT (2026-07-27 / 2026-07-28).** M8.2 arrived through the normal fork → branch → PR flow ([PR #350](https://github.com/openwave-labs/openwave/pull/350), DCO signed), the first author-driven contribution to the column. Maintainer edits were applied at merge and announced in the thread rather than sent back as a review round (the pre-registration moved to `findings/`, dates corrected, one tautological self-check replaced). The close-out exchange produced three standing rules, "Self-checks must be able to fail", "Blind vs independent-method verification" and "Three-way agreement" ([§ CONVENTIONS](#conventions)), reshaped M8.5 to run protocol-first with a context firewall and two named sub-deliverables (M8.5-A reproduction, M8.5-B backend), and turned the maintainer's review-time verification from a throwaway into a landed, quarantinable artifact ([`script`](scripts/m8_2_indep_reconstruction.py) + [note](findings/m8_2_indep_reconstruction_note.md)). The author declared the next author-side work in the same thread: the M8.5 reproduction protocol as a document before any implementation, then M8.3 under the normal reviewed workflow, which is the Backlog order above.

**M8.6 GATED: THE NAMED TARGET WAS CIRCULAR (2026-07-29).** Before pre-registering, a readiness audit traced `m8_platform_pointers.md`'s cited M5 target to its source. `1:5.9:15.1` is not independent M5 output: M5's own findings note defines it as `Λ := m^(1/3)`, calls reproducing the masses "near-tautological", and states the eigenvalue values "remain Yukawa-like input", so comparing it to the lepton masses compares a number to its own definition. The genuine measured M5 object is different: three stationary-state energies from M5.21.2/2b (`A<C<B`, `C/A≈4.2`, `B/A≈16.0`), real output with a physically-motivated ordering, but explicitly not value-converged ("quote orderings and geometry, not absolute E") and missing any physical-parameter bridge (deferred to M5.21.11). A units conversion cannot close the gap, since `C/A`, `B/A` are ratios. An ordering-only fallback was rejected too: MIT's own charged-lepton labels are assigned by mass match (`mass-spectrum.md`'s "the gates fix the kind; the mass fixes the generation"), so `m_e<m_μ<m_τ` is built into the labeling. M8.6 moves from Backlog to [LATER (gated)](#later-gated) on M5.21.11; the seven reopening conditions: [readiness note](findings/m8_6_readiness_note.md) § 8. A positive outcome, not a stalled task: it kept a rounding-artifact "0.3% match" from being pre-registered as cross-model evidence, and located the missing calibration that would make the comparison real.

**PLATFORM-SIDE CORRECTION: THE MAP THAT PLANTED THE TARGET (2026-07-29).** The audit above traced its circular target to a maintainer document, so the platform half was fixed the same day. [`m8_platform_pointers.md § 5`](m8_platform_pointers.md) now leads with the readiness note, marks `1 : 5.9 : 15.1` inadmissible (`Λ := m^(1/3)` from the masses), names the A < C < B census as the only non-circular target while flagging it not usable yet, and adds M5.21.11 as the bridge M8.6 waits on. A repository sweep for the same claim found two further live assertions of a "measured" hierarchy, in [`m8_background.md`](m8_background.md) and in the OQ4 row of [`m8_theory_canonical.md`](m8_theory_canonical.md); both corrected, OQ4 now reads gated. [`MODELS.md`](../../../../MODELS.md) and the M5 briefing passed that sweep, since both framed the ratios as an open origin rather than a result, but the test was too narrow: [#375](https://github.com/openwave-labs/openwave/pull/375) caught that each still described M8.6 itself as ready to run, and corrected both. Reverse links make the gate visible from the M5 side: the particle-hunt lepton row, and the [M5.21.11 task doc](../../m5_liquid_crystal/research/tasks/m5_21_11_task_details.md), which now records what the bridge has to deliver. The general lesson: an audit's finding belongs everywhere the claim lives, not only where the audit looked.

**CONDITION 3 AMENDED: THE EXTRAPOLATION ROUTE (2026-07-29).** Cross-linking the gate surfaced a real tension: condition 3 as written admitted only a direct run at physical parameters, but M5.21.11's own scope states the physical regime (δ ~ 1e-10, g ~ 1e10) is out of lattice reach by any direct method, so taken literally, condition 3 was unsatisfiable by any route M5 has. Resolved as "A scientifically, C procedurally": a preregistered extrapolation is the right science, which means formally amending the condition before any rung is measured, not treating extrapolation as already permitted. [Readiness note § 8](findings/m8_6_readiness_note.md) now admits route (b), a physical-regime extrapolation from a fresh, preregistered census ladder, alongside the original direct-run route (a), under guardrails that close the obvious loophole: the asymptotic form must be theory-derived independent of the lepton target, the rung set/fitting/holdout/uncertainty model frozen before any performance evaluation against the charged-lepton ratios is run, and the three existing toy energies barred from entering the new fit as data points, an exponent search, or a post-hoc transformation. A new claim-ceiling statement records what a successful route (b) result would and would not establish, even if it works perfectly.

**GATED-STATUS PROPAGATION AND THE M8.2 POST-LOCK ADDENDUM (2026-07-29).** Two prose-only follow-ups from the author, [#375](https://github.com/openwave-labs/openwave/pull/375). First, the gate's status reached three docs the platform sweep had cleared: [`MODELS.md`](../../../../MODELS.md)'s lepton row and [`__M8_model_briefing.md`](../__M8_model_briefing.md) still presented M8.6 as a ready-to-run comparison, and [`m8_background.md`](m8_background.md)'s payoff clause now follows the gate instead of preceding it. The two sweeps asked different questions: the platform's, whether a doc asserted a MEASURED hierarchy, which none did; the author's, whether a doc still described the TASK as runnable, which two did. The second is what a reader deciding whether to pick the task up actually needs. Second, the M8.2 pre-registration is locked, so its superseded `mass-null-v1.0` citation (p = 0.174) was corrected by a dated addendum rather than an in-place edit, following that document's own freeze policy. The corrected `mass-null-v1.1` (p_A = 0.690) strengthens rather than weakens the exclusion of the 24-entry mass table from M8.2's evidential scope, since the higher p makes the coverage count more consistent with random reassignment.

**M8.5-A STEP 1: THE REPRODUCTION PROTOCOL IS LOCKED (2026-07-30).** The author filed the protocol as a document before any implementation existed, [#380](https://github.com/openwave-labs/openwave/pull/380), discharging step 1 of the split agreed in the [#350](https://github.com/openwave-labs/openwave/pull/350) close-out: [`findings/m8_5a_reproduction_protocol.md`](findings/m8_5a_reproduction_protocol.md), frozen at merge with addenda-only amendment. Three things in it go past what the close-out asked for. The claim ceiling is now capped structurally for an AI implementer, since an opaque training corpus means prior exposure to published values cannot be excluded even under perfect task-time isolation, so isolation buys provenance and never the label. An integer-nearness rule gates every multiplicity before the occurrence threshold, so a numerically untrustworthy value can no longer become "present". And a maintainer packet audit guards author-side leakage, the one direction the author controls. It also answers the standing question left open at #350: the ASSERTED coexact rule is an optional adjudication module with four pre-declared verdicts, and numerical agreement in any amount never upgrades it. Review added two gates covering `Sym²` and the `V_n` tower, ungated in the frozen floor, and gave the orphaned M8.3 torsion reproduction a row ([M8.8](tasks/m8_8_task_details.md)).

**M8.5-A COMPLETE: REPRODUCED, THREE-WAY, AUDITED (2026-07-31 / 2026-08-01).** The author delivered the packet ([#386](https://github.com/openwave-labs/openwave/pull/386) thread) as two deliberately separate archives, generators exact in `Q(φ)`, so the hash pins a group rather than a transcription. The maintainer audit ran from the packet alone (eight mutation-tested checks, [#389](https://github.com/openwave-labs/openwave/pull/389)); the clean-room session implemented from the audited four-file packet; the § 9 commitment landed before anything was unsealed ([#391](https://github.com/openwave-labs/openwave/pull/391)); the § 7 adjudication followed in its own PR naming it ([#392](https://github.com/openwave-labs/openwave/pull/392)): **REPRODUCED**, 27/27 cells against the pinned § 6.1, three-way agreement, both transcription mutations reddening. The § 6 coexact module claimed `structurally derived and reproduced` on its own general argument; the recorded adversarial audit ([`m8_5a_audit.py`](scripts/m8_5a_audit.py), a Burnside-route reconstruction the implementation avoids) refuted none of eight claims and strengthened two to theorems, so the verdict stands on the derivation, the numerical match contributing nothing. Record: [method note](findings/m8_5a_method_note.md) (§§ A-H frozen, § I adjudication, § J audit), [commitment](findings/m8_5a_commitment.md), [task details](tasks/m8_5_task_details.md). The reusable clean-room procedure closes separately as T4 on the [platform roadmap](../../../../dev_docs/platform_roadmap.md). M8.5-B is the live remainder, its certification gate discharged.

**M8.5-B PRE-REGISTRATION LOCKED (2026-08-01 / 2026-08-02).** The author filed the backend's numerical pre-registration before any implementation, [#402](https://github.com/openwave-labs/openwave/pull/402): [`findings/m8_5b_preregistration.md`](findings/m8_5b_preregistration.md). Two routes over a frozen finite two-sided deck action, four named evidence classes, frozen claim phrasings, a seven-rung ladder whose 3a/3b adjudication runs against a sealed two-packet case with both hashes published before landing, and § 6 parameters pilot-selected with the measured margin printed beside every tolerance. Review ran one round under [`PR_REVIEW_STANDARDS.md § 10.3`](../../../../dev_docs/PR_REVIEW_STANDARDS.md): the one blocking finding was the unnamed adjudicator, the #380 who-pays lesson caught BEFORE the freeze this time, answered in-thread (the author adjudicates). The close-out strengthened the contract: packets are published after adjudication as sequence step 7, converting the hashes from private commitments into checkable ones; G5a moved from transcription to direct ingestion of the hash-verified packet applying its machine-readable `indexing_map`; and a synthetic map-consumption fixture was added after the author caught that a live case may not discriminate applying the map from ignoring it. The § 11 external sources were registered in [`theory/_CITATIONS.md`](../theory/_CITATIONS.md). LOCKED at merge (`d5c967c0`); the B build is ungated.

**THE M8.6 BRIDGE PRE-REGISTRATION DELIVERED AND FROZEN (2026-08-06 → 2026-08-07).** M5.21.11 produced the route-(b) artifact the amended condition 3 requires: [`m5_21_11_framework.md`](../../m5_liquid_crystal/research/findings/m5_21_11_framework.md), carrying the theory-derived asymptotic form, the frozen rung/holdout/uncertainty protocol (including the condition-4 refinement subset and its propagation rule), the barred-inputs audit, and a § 8 table mapping each reopening condition to the section satisfying it. FROZEN 2026-08-07 with a pinning commit SHA, after an M5-side author sanity check of the derived forms (the practical-approximation route endorsed, no form challenged); any further edit voids route (b). Two M8-side notes recorded in the [gate record, FINDINGS 7-8](tasks/m8_6_task_details.md): the condition-4 "subset spanning the ladder" is satisfied literally including both endpoint rungs (rung cost measured δ-independent, so the anticipated affordability amendment was never needed), and the gate does NOT reopen at the freeze: reopening requires the ladder run completed under the frozen framework with its holdout and terminal failure gates passed. No status change: the row stays LATER (gated), with the Gated By cell now pointing at the frozen framework.

**M8.6 CLOSED ON THE M5 SIDE: THE LADDER RAN AND ROUTE (b) FAILED TERMINALLY (2026-08-07).** The frozen program executed the day of the freeze (M5-side record: [`m5_21_11_task_details.md § LADDER RUN`](../../m5_liquid_crystal/research/tasks/m5_21_11_task_details.md)) and two pre-committed terminal criteria fired. The g-arm measured the 4D boost-dressing gain FLAT in g across the frozen rungs (the framework's one-dimensional-in-δ reduction, the object the extrapolation rested on, fails by measurement), and the frozen instrument-health gates certified 1/0/0 usable rungs for the three branches against the 6-per-branch floor, the two heavier branches, the μ- and τ-candidates this comparison needed most, uncertifiable at every rung. No fit ran and no ratio was produced. Under the framework's own § 6 the failure is terminal: route (a) was already out of lattice reach, route (b) was the last admissible route, and the amended condition 3 admits no third. The Gated By cell now records the gate as CLOSED on the M5 side ([finding 9](tasks/m8_6_task_details.md)); the row stays [LATER (gated)](#later-gated) as the permanent state unless a future M5 instrument generation, which no existing task promises, delivers a fundamentally different certified route. The M5-side release directive (Backlog on a passing close) therefore did not fire.

**M8.6 ROW RETIRED TO DONE (2026-08-08, maintainer call).** With the gate closed permanently on the M5 side, holding the row in LATER (gated) implied a wait that can never end, so it moved to the Done record as CLOSED WITHOUT RUNNING: the task's actual deliverable (the readiness audit that caught the circular target, plus the gate record through finding 9) is complete and final. Bookkeeping only; no scope or evidence change.

**M8.8 PROTOCOL AND CONSTRUCTION PACKET MERGED, THE FREEZE STILL PENDING (2026-08-11).** The author filed the reproduction protocol and the based chain complex it opens from as one pull request, [#408](https://github.com/openwave-labs/openwave/pull/408): [`findings/m8_8_reproduction_protocol.md`](findings/m8_8_reproduction_protocol.md) + [`data/m8_8_construction_packet.json`](data/m8_8_construction_packet.json). Review rebuilt the packet from the two public packets alone, 35 mutation-tested gates and no torsion value, and requested two text changes. The load-bearing one: § 9's universal-cover gate stated its conclusion in all four degrees while establishing degree 2 alone, leaving degrees 0 and 1 on the rational ranks, which are blind to finite index. The gate now demands an exact saturation certificate for `im d1`, `im d2` and `im d3`. The construction review offered as evidence, `2 - z` on one `d2` row and the complementary `d3` column, measured later as a NULL changing no homology; the gap is real, the reaching construction `d2 -> (6 - 5z) d2` ([task details](tasks/m8_8_task_details.md)). § 4.4's `convention_map` wording, which had already produced a defect, became four explicit items, and a new § 4 paragraph makes implementer ineligibility permanent. The merge does NOT freeze the document: § 12 binds from the § 8 lock commit, leaving § 11's five pins fillable in place. Record: [`tasks/m8_8_task_details.md`](tasks/m8_8_task_details.md).

**M8.8 PRE-LOCK CLEANUP: THE PINS FILLED AND THE PACKAGING CLAUSE CORRECTED (2026-08-11).** [#430](https://github.com/openwave-labs/openwave/pull/430) merged the same day, protocol text only. § 11's five pins are filled, the four maintainer-derivable values recomputed at the head rather than transcribed, and its drafted-against row now records the commit the protocol was written from rather than the pull request's base. § 10.3 no longer requires the run to land as one pull request: the artifacts MAY land across separate commits or requests, merge order need not match § 8 execution order, and binding is by the hash commitments and manifests § 8 requires. Removing that clause orphaned four definite referents (§ 8 steps 1 and 2, § 10.3, § 12), all repaired in review. § 12's own closing clause proved older than the change: "the pinned paths of § 11" had no referent in any of the eight versions of the file, § 11 having never carried a path, and now reads that the pinned VALUES may not change after the lock, renaming explicitly not a breach and substitution explicitly one. The protocol is settled text at `a6730e82…`, both packets untouched. Record: [`tasks/m8_8_task_details.md`](tasks/m8_8_task_details.md).

**M8.5-B PRE-REVEAL STOP AND REPAIR (2026-08-16).** The first real adjudication case, `M85B-ADJ-04`, was sealed by an isolated unit and committed under [Addendum 12.2](findings/m8_5b_preregistration.md). Packet I opened and verified; at § 4.1 step 3 route (a) refused before producing any output, because its multiplication-table lookup required sign-exact equality of action-pair representatives while the effective-group closure identifies them modulo the diagonal central kernel. Route (b) was not run, no route output was committed, Packet II was never opened, and no external reference value was consulted, so no adjudication occurred and the external test is unscored. The § 6.1 tuning case `L(7;1,2)` fails identically, so the defect was latent machinery rather than case-induced; it survived qualification because every case that previously reached route (a)'s stencil path was sign-exact closed by accident of selection. [Addendum 12.3](findings/m8_5b_preregistration.md) authorized the one permitted repair, `M85B-ADJ-04` retired as the pre-reveal bug-discovery case with its Packet II sealed indefinitely, and `research/m8_5b/route_a_closure_battery.py` now carries the missing coverage, with a mutation arm and reviewer-side re-mutation confirming the repair is load-bearing ([#444](https://github.com/openwave-labs/openwave/pull/444)). Maintainer review and re-freeze are discharged, so the next fresh case selection is un-gated.

**M8.5-B COMPLETE: THE FIRST SEALED-CASE ADJUDICATION, GREEN (2026-08-17).** A fresh case sealed by an isolated unit was adjudicated end to end against the frozen, published Phase A machinery ([#446](https://github.com/openwave-labs/openwave/pull/446)). The ordering is carried in commit ancestry, not attestation: both packet hashes were published in [Addendum 12.4](findings/m8_5b_preregistration.md) while the packets were sealed, both frozen routes then ran and committed raw outputs with the answer packet unread, and only then was it hash-verified and opened. **Rung 3a GREEN both routes**, the complete sealed scalar multiplicity vector at exact integer equality through the packet-owned band, zero cells included. **Rung 3b GREEN both routes.** The maintainer re-derived the answer by his own character computation, reran both routes and rungs, attacked the comparator, and verified 37/37 pins unmoved. Limits stay attached: published-value adjudication, not an unseen-target claim; rung 3b against route (b) is theorem-consistency, not a third derivation; the homogeneous case does not exercise the [Addendum 12.3](findings/m8_5b_preregistration.md) repair. `M85B-ADJ-04` stopped pre-reveal on a latent route-(a) defect; `M85B-ADJ-06` was retired pre-route on commitment ordering. Record: [`M85B-ADJ-07/ADJUDICATION_RECORD.md`](m8_5b_adjudication/M85B-ADJ-07/ADJUDICATION_RECORD.md).

**THE M8.4 KINEMATIC CLOSE: OQ1'S NATIVE BRANCH IS A THEOREM (2026-08-18).** The task's first
result is analytic. Every single-valued field on S³/2I lifts 2I-invariant, so its
deck-isotypic content is trivial: native quotient states carry none of the 8 nontrivial McKay
slots at any harmonic level. The spinorial half, the electron slot included, is absent by
parity alone (everything on the quotient is `(−1)`-even; those slots live at odd levels); the
nonconstant invariant support starts at level 12. The control confirms the full `n = d`
first-occurrence ladder in the twisted sectors, exactly where the M8.2 § 3 tables put it.
Record: [finding](findings/m8_4_kinematic_close.md) + [script](scripts/m8_4_kinematic_check.py),
two computationally separate routes (icosian characters; the McKay recursion), mutation-armed,
exit 0.
Consequence: dynamics cannot overcome absence from the configuration space, so stages 1-5
decouple into an optional descriptive survey and the slot survey retargets onto the M8-owned
twisted object `M4_int` across the three frozen flat connections, `σ_0` the mandatory null
control, no post-hoc connection selection or pooling, the free ladder scored as calibration
and never as evidence. The `M4_int` preregistration is the next artifact; `M5 + P` and
`M7_ad` stay out.

**M8.8 ADJUDICATED: THE TORSION CLOSED FORMS REPRODUCED UNDER THE GLOBAL INVERSE (2026-08-22).** The author delivered the answer packet on an orphan commit ([#408](https://github.com/openwave-labs/openwave/pull/408) thread); the maintainer decrypted it alone and its plaintext hashed to the § 11 pin `744c7f25…`. The committed pre-reveal harness then REFUSED at the step-7 packet-domain gate on a one-string interface mismatch, a maintainer placeholder against the author's frozen builder literal: attempt 1 is on record as `structural failure`, nothing past the gate having run. [#457](https://github.com/openwave-labs/openwave/pull/457) replaced the literal and nothing else in the comparison, its commit message fixing attempt 1's disposition before any category was disclosed, and the official run on `main` returned **`convention difference`**: the global inverse at `R7`, then 8/8 rows and 4/4 identities exact in `Q(φ)`, record `a6036744…` byte-identical to a control built from the refused harness plus the one substitution. Step 9 published the packet and the construction-audit artifact `d5bb04b9…` against their frozen hashes. The protocol gap on post-reveal comparator repair is recorded, not amended, and declared the last such repair. Record: [`findings/m8_8_adjudication_record.md`](findings/m8_8_adjudication_record.md).
