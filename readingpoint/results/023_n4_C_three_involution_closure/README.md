# Result 023 — N4 `C` three-involution closure

## Outcome

**FIELD-LEVEL CLOSURE: `C2^3`-LIKE SUPPORTED.**

**DISTINCT TRANSFORMATIONS: 8.**

**UNIQUE `V4` SELECTION: NOT ESTABLISHED.**

**REPRESENTATION ON `C`: NON-FAITHFUL `Z2` SIGN REPRESENTATION.**

Result 022 established two V4-like candidate pairs involving the tested
involutions

`T_x = P o R_x`

`T_y = P o R_y`

`T_z = P o R_z`.

Result 023 resolves whether those pairs belong to one common four-element
group or sit inside a larger closure.

The full closure contains eight distinct field transformations.

## Generated closure

The explicitly generated words are:

`I`

`T_x`

`T_y`

`T_z`

`T_x T_y`

`T_x T_z`

`T_y T_z`

`T_x T_y T_z`

All eight are distinct at field level.

## Generator relations

Each generator squares to identity:

`T_x^2 = I`

`T_y^2 = I`

`T_z^2 = I`.

They also commute pairwise:

`[T_x, T_y] = 0`

`[T_x, T_z] = 0`

`[T_y, T_z] = 0`.

Thus the tested closure has the composition pattern of three independent
commuting order-2 generators.

The appropriate current classification is:

**`C2^3`-LIKE FIELD-LEVEL CLOSURE.**

## Distinct-state count

The clustering test finds:

`distinct transformations = 8`.

The eight generated words occupy eight distinct classes:

- `I`
- `T_x`
- `T_y`
- `T_z`
- `T_x T_y`
- `T_x T_z`
- `T_y T_z`
- `T_x T_y T_z`

No collapse to a unique four-element set occurs.

## Representative field distances

Selected distances from the identity are:

`I - T_x = 1.012493e+00`

`I - T_y = 9.525433e-02`

`I - T_z = 1.074153e+00`

`I - T_x T_y = 1.012946e+00`

`I - T_x T_z = 1.263628e+00`

`I - T_y T_z = 1.074153e+00`

`I - T_x T_y T_z = 1.263939e+00`.

All exceed the distinctness threshold by a wide margin.

## Action on `C`

The eight field transformations collapse to only two actions on the
effective chiral matrix.

The observed sign actions are:

| Field transformation | Action on `C` |
| --- | --- |
| `I` | `+C` |
| `T_x` | `-C` |
| `T_y` | `+C` |
| `T_z` | `+C` |
| `T_x T_y` | `-C` |
| `T_x T_z` | `-C` |
| `T_y T_z` | `+C` |
| `T_x T_y T_z` | `-C` |

Therefore:

`+C actions = 4`

`-C actions = 4`

`neither = 0`.

## Non-faithful representation

The field-level closure contains eight distinct transformations, but `C`
sees only two signs.

Thus the induced action on `C` is:

**NON-FAITHFUL `Z2` SIGN REPRESENTATION OF THE `C2^3`-LIKE FIELD CLOSURE.**

The kernel of this sign readout contains four distinct field
transformations.

Therefore `C` cannot distinguish:

- all eight field-level transformations;
- a unique four-element subgroup;
- or a four-way physical state classification.

## Resolution of Result 022 ambiguity

Result 022 showed that both pairs

`<T_x, T_y>`

and

`<T_x, T_z>`

form V4-like four-state subsets.

Result 023 now shows why two such candidates appeared:

they are embedded inside a larger eight-element closure.

So the tested M5/N4 transformation structure does **not** naturally select
one unique V4 from these generators.

This is a crucial bridge constraint.

## Relation to Result 003

Result 003 established the mathematical relation

`(Z/30Z)* / {1,19} ≅ Q8 / {1,-1} ≅ C2 x C2`.

Result 023 does not identify the new M5/N4 field closure with that quotient.

Instead, the currently tested field-level structure is larger:

`C2^3`-like.

Such a structure contains multiple four-element V4 subgroups.

Therefore selecting one of those V4 subgroups to match the Result 003
quotient would require an additional independently defined criterion.

Choosing one merely because it has four elements would repeat the kind of
arbitrary cardinality-based bridge rejected in Results 001–003.

## Relation to `C`

The effective N4 chiral matrix does not solve the subgroup-selection
problem.

Its sign action reduces the eight field transformations to two classes:

`+C`

and

`-C`.

That readout is too coarse to distinguish four states.

So although `C` is now numerically well characterized in shape,
normalization, and symmetry action, it does not provide the missing
four-way operational quotient.

## Result

The executable test establishes:

**Three independent involutions:** SUPPORTED

**Pairwise commutation:** SUPPORTED

**Eight distinct field transformations:** SUPPORTED

**Field-level closure:** `C2^3`-LIKE

**Unique V4 selection:** NOT ESTABLISHED

**Representation on `C`:** NON-FAITHFUL `Z2`

**Four-way classification from `C`:** NOT ESTABLISHED

**`Q8/{+1,-1}` identification:** NOT ESTABLISHED

**Reading Point -> M5 physical mapping:** NOT ESTABLISHED

## Interpretation

The M5/N4 side now has a richer explicitly tested discrete structure than
the four-element quotient originally sought.

That is useful, but it makes the bridge requirement stricter.

A Reading Point or `Q8/{+1,-1}` comparison now needs an independent reason
to select a particular four-state reduction from the eight-state
field-level closure.

The single effective observable `C` cannot supply that reason because its
representation is non-faithful and binary.

## Constraint

Result 023 does not establish:

- particle classes;
- quaternion defect classes;
- physical handedness;
- a unique `V4`;
- `Q8/{+1,-1}`;
- a Reading Point residue assignment.

The label `C2^3`-like describes the tested transformation algebra only.

## Current bridge status

**N4 `C` normalized shape:** SUPPORTED

**N4 lattice normalization `dx*C`:** SUPPORTED

**N4 antisymmetry:** SUPPORTED

**`T_x`, `T_y`, `T_z`:** SUPPORTED

**Full field closure:** `C2^3`-LIKE

**Embedded V4-like subgroups:** MULTIPLE

**Unique V4 selection:** NOT ESTABLISHED

**Representation on `C`:** NON-FAITHFUL `Z2`

**Four-way operational classification from `C`:** NOT ESTABLISHED

**`Q8/{+1,-1}` identification:** NOT ESTABLISHED

**Reading Point -> M5 physical mapping:** NOT ESTABLISHED

## Next reading point

Result 024 should stop adding generators and ask whether the existing M5/N4
repository already contains a **second independently defined observable**
that resolves states identified by `C`.

The immediate question is:

> Is there an existing observable whose values, together with the `C` sign,
> distinguish a non-arbitrary four-state quotient or select one embedded
> V4 subgroup from the `C2^3`-like closure?

The observable must already have an independent M5 definition or physical
motivation.

It should not be invented solely to manufacture four classes.

If no such observable exists in the examined implementation, that would be
a meaningful stopping boundary for the current Reading Point -> M5 bridge.

## Script

`readingpoint/tests/test_023_n4_C_three_involution_closure.py`
