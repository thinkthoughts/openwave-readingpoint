# Result 024 — N4 second independent observable from the real-overlap sector

## Outcome

**`Mr` ADDS NO INDEPENDENT DISCRIMINATION WITHIN THE `C`-SIGN SECTORS.**

**JOINT CLASS COUNT: 2.**

**MEASURED EQUIVALENCE IS COMPOSITION-COMPATIBLE.**

**INDUCED EFFECTIVE QUOTIENT: `C2`.**

Result 023 established an eight-state, `C2^3`-like field-level closure
generated by

`T_x`, `T_y`, and `T_z`.

The N4 chiral matrix `C` sees only two signs across that closure:

- four transformations act as `+C`;
- four transformations act as `-C`.

Result 024 asks whether the already implemented N3/N4 real-overlap sector
adds an independent discriminator.

It does not.

## Existing observable tested

The preregistered second observable is the real N3/N4 flavour-space sector

`Mr = K + kappa P`

with

`K_ab = <grad dM_a, grad dM_b>_s`

and

`P_ab = <dM_a, dM_b>_s`.

For the current N4 baseline,

`kappa = 0`

so

`Mr = K`.

The test recomputes `Mr`, `K`, `P`, and `C` directly from each transformed
field configuration.

No classifier is fitted and no subgroup is selected in advance.

## Tested closure

The eight Result-023 transformations are:

`I`

`T_x`

`T_y`

`T_z`

`T_x T_y`

`T_x T_z`

`T_y T_z`

`T_x T_y T_z`.

## Real-overlap result

For every transformation,

`||Mr|| = 3.785515e+03`.

The normalized `Mr` matrices are identical to numerical precision.

The pairwise normalized-`Mr` distances range from:

`0`

to approximately

`3.14e-16`.

Thus the real-overlap sector is effectively invariant under the full tested
closure.

## Symmetry controls

`Mr` remains exactly symmetric in the reported test:

`Mr symmetry error = 0`.

`C` remains antisymmetric at machine precision, with maximum reported error
of order:

`10^-15`.

Therefore the negative discrimination result is not caused by a failure of
the expected matrix symmetry classes.

## Equal-`C`-sign discrimination

The critical question is whether `Mr` separates transformations that `C`
identifies.

Inside the `+C` and `-C` sectors there are 12 equal-sign pairs.

Number separated by normalized `Mr`:

`0 / 12`.

Therefore:

**`Mr` ADDS NO INDEPENDENT DISCRIMINATION WITHIN THE `C`-SIGN SECTORS.**

## Joint signatures

The measured joint signature rule is:

same `C` sign

and

normalized-`Mr` distance `<= 1e-4`.

The resulting classes are:

### Class 0

`I`

`T_y`

`T_z`

`T_y T_z`

### Class 1

`T_x`

`T_x T_y`

`T_x T_z`

`T_x T_y T_z`

So the joint class count remains:

**2**

rather than 4 or 8.

## Quotient compatibility

The test separately checks whether this measured equivalence relation is
compatible with the already established `C2^3`-like composition law.

It is.

Number of compatibility violations:

`0`.

The induced class multiplication table is:

```text
    0  1
0   0  1
1   1  0
```

Therefore the effective two-class readout defines the expected:

**`C2` QUOTIENT.**

## Interpretation

The N4 real sector and chiral sector behave very differently under the
tested field transformations.

The real overlap matrix `Mr` is invariant.

The chiral matrix `C` supplies a binary sign.

Together they still distinguish only:

`+`

and

`-`.

Thus the already implemented complex effective structure

`M_H = Mr + i g_chiral C`

does not expose a four-state readout under the tested closure.

The real part adds no independent transformation label.

## Consequence for V4 selection

Result 023 showed that the field-level `C2^3`-like closure contains multiple
embedded V4 subgroups.

A second independent observable could, in principle, have selected a
non-arbitrary four-state reduction.

`Mr` does not do that.

Therefore:

**UNIQUE V4 SELECTION: NOT ESTABLISHED.**

Choosing one embedded V4 subgroup would still require an independent
criterion not supplied by the tested real-overlap sector.

## Relation to Result 023

Result 023 found:

- eight distinct commuting field transformations;
- a `C2^3`-like closure;
- four `+C` actions;
- four `-C` actions;
- a non-faithful `Z2` sign representation on `C`.

Result 024 tests the most natural second effective observable already
present beside `C` in the N4 mass matrix.

It remains invariant across all eight transformations.

So the effective N3/N4 mass-matrix machinery continues to expose only the
binary quotient already visible through the sign of `C`.

## Bridge consequence

The current tested hierarchy is now:

field-level structure:

`C2^3`-like, 8 states;

effective `C` readout:

`C2`, 2 signs;

effective `C + Mr` readout:

still `C2`, 2 classes.

No four-way operational structure has emerged from these existing effective
observables.

Therefore there is still no non-arbitrary identification with the
four-element quotient

`Q8/{+1,-1}`

or with the previously examined Reading Point quotient.

## Result

The executable test establishes:

**`Mr` independent discriminator:** REJECTED

**Equal-`C`-sign pairs separated by `Mr`:** `0/12`

**Joint class count:** `2`

**Measured equivalence composition-compatible:** YES

**Induced quotient:** `C2`

**Unique V4 selection:** NOT ESTABLISHED

**`Q8/{+1,-1}` identification:** NOT ESTABLISHED

**Reading Point -> M5 physical mapping:** NOT ESTABLISHED

## Constraint

This result uses the tested reference geometry and the N4 baseline

`kappa = 0`.

It does not prove that every possible real-sector deformation is invariant.

It does establish that the existing baseline real-overlap observable does
not supply the missing discriminator in the tested implementation.

No new observable or fitted partition was introduced.

## Current bridge status

**Field-level closure:** `C2^3`-LIKE

**`C` readout:** NON-FAITHFUL `Z2`

**`Mr` readout:** INVARIANT

**Joint `C + Mr` readout:** TWO CLASSES

**Induced effective quotient:** `C2`

**Unique V4 selection:** NOT ESTABLISHED

**Four-way operational classification:** NOT ESTABLISHED

**`Q8/{+1,-1}` identification:** NOT ESTABLISHED

**Reading Point -> M5 physical mapping:** NOT ESTABLISHED

## Next reading point

Result 025 should examine the other independently implemented geometric
sector already identified in Result 015:

the M5 connection / curvature machinery.

The question should be narrow:

> Does an existing connection- or curvature-based observable distinguish
> field transformations that the joint `C + Mr` readout identifies?

The observable must already have an independent M5 definition.

A new discriminator should not be invented merely to manufacture four
classes.

If the repository contains no defined projection from connection/curvature
geometry onto these transformed N4 flavour states, that absence itself is a
meaningful result and may mark the stopping boundary for the current
Reading Point -> M5 bridge.

## Script

`readingpoint/tests/test_024_n4_second_observable_real_overlap.py`
